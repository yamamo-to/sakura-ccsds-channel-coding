# AGENTS.md - AI マルチエージェント ガイドライン for CCSDS チャネル符号化 (Python)

---

## 1. プロジェクト概要 & ディレクティブ

- **プロジェクト名**: `ccsds-codec`
- **対象言語/バージョン**: Python 3.11+
- **主要依存**: `numpy`, `pytest`, `numba`（性能が重要なデコーダで使用）
- **主目的**: CCSDS 推奨規格 **CCSDS 131.0‑B‑4 (TM 同期とチャネル符号化)** に完全に準拠したエンコーダ／デコーダを実装すること。
- **品質目標**
  1. 多項式・コードレート・ビット順序を **厳密に遵守**
  2. ソフト判定 / Log‑MAP デコーダで **数値的安定性** を確保（浮動小数点アンダーフローなし）
  3. `numpy` のベクトル化と `numba` の JIT による **高速化**

---

## 2. CCSDS 規範的技術仕様

AI エージェントは以下の定数・パラメータ **そのまま** を使用し、代替や簡易版は一切使用しないこと。

### A. Reed‑Solomon コード: RS(255, 223)
- **ガロア体**: $GF(2^8)$、原始多項式 `p(x) = x^8 + x^7 + x^2 + x + 1`
- **生成多項式**: $g(x)=\displaystyle\prod_{j=112}^{143}(x-\alpha^j)$（$\alpha$ は $GF(2^8)$ の原始元）
- **インタリービング深さ ($I$)**: $1 \le I \le 5$（ブロックインタリーブ）
- **表現形式**: 従来表現（Dual‑basis 変換はオプション／設定可能）

### B. 畳み込み符号 (CONV)
- **制約長 ($K$)**: 7
- **コードレート ($R$)**: $1/2$（必要に応じて $2/3, 3/4, 5/6, 7/8$ のパンクチャをサポート）
- **生成多項式**（CCSDS 131.0-B-4 / 101.0-B-4 準拠）
  - $G_1 = 171_8$（$1111001_2$、第1出力シンボル、非反転）
  - $G_2 = 133_8$（$1011011_2$、第2出力シンボル、**チャネル上で反転**）
  - ※ 実装は lsb-current 表現（シフトレジスタの LSB が最新ビット）。コード上の定数は
    $G_0 = 0x4F$（$G_1=171_8$ のビット反転）、$G_1 = 0x6D$（$G_2=133_8$ のビット反転）を使用し、
    第2出力を反転する。gr-satellites の GNU Radio `fec.cc_decoder`（polys `[79, -109]`）と
    ビット一致する。
- **復号**: ハード判定／ソフト判定（Euclidean / LLR メトリック）を用いた Viterbi アルゴリズム

### C. Turbo コード
- **構成コード**: 系統的再帰畳み込み (RSC) コード、制約長 $K=5$
  - フィードバック多項式: $g_0 = 10011_2$（$23_8$）
  - フォワード多項式:   $g_1 = 11011_2$（$33_8$）
- **基礎レート**: $1/2, 1/3, 1/4, 1/6$
- **情報ブロック長 ($K_{bits}$)**: 1784, 3568, 7136, 8920, 16384 ビット
- **インタリーバ**: CCSDS 付録の **Quadratic‑Permutation** アルゴリズム（ブロック長ごとに決められた $k_1, k_2$ 定数テーブル）
- **復号**: 反復的 Log‑MAP もしくは Max‑Log‑MAP（最大 10 回反復）

---

## 3. エージェントの役割 & 運用責任

### 🤖 Agent 1 – Architect（仕様判定・設計）
- **責務**
  - `src/ccsds/` 配下のインターフェース・モジュール構造・データフロー設計
  - データは **1次元 `numpy.ndarray`**（`uint8` ビット列または `float64` LLR）で統一
  - 抽象基底クラス `BaseEncoder`, `BaseDecoder` を定義

### 🤖 Agent 2 – Coder（アルゴリズム実装）
- **責務**
  - ガロア体演算、LFSR、トレリス/BCJR などの数学的ルーチン実装
  - まずは **`numpy` ベクトル化**、ボトルネックは `@numba.njit` で JIT コンパイル
  - 明示的型ヒントは必ず付与（`numpy.ndarray`, `float64` など）

### 🤖 Agent 3 – Tester（検証・テスト作成）
- **責務**
  - `tests/` に **公式テストベクトル**（CCSDS 付録）を使ったテストスイートを作成
  - AWGN チャネル上で Monte‑Carlo **BER / FER** シミュレーションを実施
  - 0 フレーム、ランダムノイズ、バーストエラーなどエッジケースを必ず網羅

### 🤖 Agent 4 – Reviewer（コードレビュー・品質保証）
- **責務**
  - ビットエンディアン（MSB‑first vs LSB‑first）不一致をチェック
  - Log‑MAP の LLR 計算で $\exp$ のオーバーフロー / アンダーフロー防止を確認
  - `ruff` / `black` / `flake8` による **PEP 8** 準拠、ドキュメント品質の保証

---

## 4. コーディング & 実装ルール

### 1. データ型基準
- **ビット列**は `np.ndarray`、dtype `np.uint8`、要素は必ず `0` または `1`
- **ソフトシンボル / LLR**は `np.ndarray`、dtype `np.float64`（正の値＝`0` の尤度、負の値＝`1` の尤度）

### 2. 性能・速度
- Viterbi/BCJR デコーダ内のビット単位処理は **純粋 Python ループを禁止**
- 計算カーネルは `@numba.njit(fastmath=True)` を付与して JIT 化

### 3. エラーハンドリング & ドキュメント
- すべての public 関数は **Google 形式 docstring** を必須とし、数式や CCSDS 章番号への参照を記載すること

```python
def viterbi_decode(
    rx_llrs: np.ndarray,
    constraint_len: int = 7,
    gens: tuple[int, int] = (0x4F, 0x6D)  # lsb-current: CCSDS G1=171_8, G2=133_8
) -> np.ndarray:
    """Log‑MAP デコーダで LLR ストリームを Viterbi アルゴリズムで復号する。

    Args:
        rx_llrs: float64 の Log‑Likelihood Ratio 配列（1 次元）
        constraint_len: 制約長 K（デフォルトは CCSDS の 7）
        gens: Octal 形式の生成多項式 (G1, G2)

    Returns:
        デコードされたバイナリフレーム（uint8 の 1 次元 ndarray）
    """
```

## 5. 実装上の共通制約 & 黄金律

1. **ビット/バイト順序**: すべての入出力・ビット列操作は CCSDS 標準に準拠した **MSB-first** を厳守する。暗黙的なエンディアン変換を行わないこと。
2. **Numba 連携の原則**: 計算コアは Numba (`@numba.njit`) 最適化のため、Python のオブジェクトやクラスを引数に取らず、**NumPy 配列とプリミティブ型のみ**を受け渡す関数設計にすること。
3. **Golden Vector 検証**: 新規エンコーダ・デコーダ実装時は、必ず CCSDS 公式仕様書に記載されているサンプルデータ（または独立した信頼できるツールで生成したテストベクトル）との一致を確認するテストを義務付ける。
4. **数値安定性**: 対数領域（Log-domain）演算では、`np.logaddexp` や適切な $\max^*$ 補正項を使用し、アンダーフロー／オーバーフローを防ぐこと。

---
